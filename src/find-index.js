/**
 * @template T
 * @param {(value: T, index: number, array: T[]) => unknown} callbackfn
 * @returns {number}
 */
function findIndex(callbackfn) {
  // your implementation
}

module.exports = { findIndex };
