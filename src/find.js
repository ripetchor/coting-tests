/**
 * @template T
 * @param {(value: T, index: number, array: T[]) => unknown} callbackfn
 * @returns {T | undefined}
 */
function find(callbackfn) {
  // your implementation
}

module.exports = { find };
