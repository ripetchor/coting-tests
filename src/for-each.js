/**
 * @template T
 * @param {(value: T, index: number, array: T[]) => void} callbackfn
 * @returns {void}
 */
function forEach(callbackfn) {
  // your implementation
}

module.exports = { forEach };
