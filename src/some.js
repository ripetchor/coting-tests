/**
 * @template T
 * @param {(value: T, index: number, array: T[]) => unknown} callbackfn
 * @returns {boolean}
 */
function some(callbackfn) {
  // your implementation
}

module.exports = { some };
