/**
 * @template T
 * @param {number} start
 * @param {number} [deleteCount]
 * @param {T[]} [items]
 * @returns {T[]}
 */
function splice(start, deleteCount, ...items) {
  // solution
}

module.exports = { splice };
